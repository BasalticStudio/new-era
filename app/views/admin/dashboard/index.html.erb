<% if flash[:notice] %>
<div><%= flash[:notice] %></div>
<% end %>
<div>
  <%= render LinkButtonComponent.new(label: t('.refresh_allowlist'), target: admin_refresh_allowlist_path, method: :post) %>
</div>
